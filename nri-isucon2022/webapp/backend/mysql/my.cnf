[mysqld]
character-set-server=utf8mb4
# MySQL 8.0 では local_infile がデフォルト無効。LOAD DATA LOCAL INFILE(初期データ投入)に必要
local_infile=1
[mysql]
default-character-set=utf8mb4
local-infile=1
[client]
default-character-set=utf8mb4
